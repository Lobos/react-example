import React, { Component, PropTypes } from 'react'

class Book extends Component {
  constructor(props) {
    super(props)
    this.state = {}
  }

  render() {
    return (
      <div>书籍列表</div>
    )
  }
}

Book.propTypes = {
}

Book.defaultProps = {
}

export default Book
